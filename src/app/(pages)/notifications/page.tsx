import { Metadata } from 'next';
import React from 'react'

export const metadata: Metadata = {
  title: "Notifications",
  description: "Stay updated with likes, comments, and new followers.",
};

export default function Notif() {
  return (
    <div>Notif</div>
  )
}
